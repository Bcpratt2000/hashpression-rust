This is ultimately going to be a file compression command line utility that will divide the file into blocks and then hash each block so that the output file will be smaller than the input. decompressing will go through every combination of characters until it regenerates all of the blocks
